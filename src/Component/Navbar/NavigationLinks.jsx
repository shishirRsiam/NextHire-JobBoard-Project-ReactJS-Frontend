import React, {useState, useEffect} from "react";
import { Link } from "react-router-dom";

const Navigation = (props) => {  // Corrected the typo here
    return (
        null
    );
}

export default Navigation;  // Corrected the typo here as well
